# Cloudflare AI Crawl Control "Managed robots.txt" is currently OFF, so this
# file is served as-is. To re-enable AI bot blocking by category, turn it on at
# Cloudflare dashboard -> AI Crawl Control -> Directives.
# Current posture: allow all crawlers (including AI) by default; block
# only admin, internal API, and the design-system scratch area.

User-agent: *
Disallow: /admin/
Disallow: /api/
Disallow: /design-system/

Sitemap: https://www.wabashsystems.com/sitemap.xml
